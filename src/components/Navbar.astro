<nav
  class="h-16 relative z-[999] top-0 left-0 right-0 flex justify-center items-center backdrop-blur-sm"
>
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 w-full">
    <div class="flex justify-center items-center h-full">
      <!-- Menú en pantallas grandes -->
      <div class="hidden md:flex">
        <div class="flex items-center gap-8">
          <a
            href="/"
            class="text-sm font-medium text-[#FFB800] hover:text-neutral-200"
            >Inicio</a
          >
          <a
            href="/blog"
            class="text-sm font-medium text-[#FFB800] hover:text-neutral-200"
            >Blog</a
          >
          <div class="relative group">
            <a
              href="#"
              class="text-sm font-medium text-neutral-300 cursor-not-allowed flex items-center gap-1"
            >
              Proyectos
              <svg
                class="h-4 w-4 text-neutral-300"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
              </svg>
            </a>
            <span
              class="absolute top-full left-1/2 transform -translate-x-1/2 bg-black text-white text-xs rounded px-2 py-1 opacity-0 group-hover:opacity-100 transition-opacity"
            >
              Próximamente
            </span>
          </div>
        </div>
      </div>

      <!-- Botón menú móvil -->
      <div class="md:hidden">
        <button
          id="menu-button"
          class="inline-flex items-center justify-center text-gray-400 hover:text-white focus:outline-none"
        >
          <svg
            id="menu-icon"
            class="h-6 w-6 transition-transform duration-300"
            stroke="currentColor"
            fill="none"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M4 6h16M4 12h16M4 18h16"></path>
          </svg>
        </button>
      </div>
    </div>
  </div>

  <!-- Menú móvil -->
  <div
    id="mobile-menu"
    class="md:hidden text-center fixed top-16 left-0 right-0 bg-black bg-opacity-90 p-4 hidden transition-all duration-300"
  >
    <a href="/" class="block text-[#FFB800] hover:text-neutral-200 py-2"
      >Inicio</a
    >
    <a href="/blog" class="block text-[#FFB800] hover:text-neutral-200 py-2"
      >Blog</a
    >
    <a href="#" class="block text-neutral-500 py-2 cursor-not-allowed"
      >Proyectos (Próximamente)</a
    >
  </div>
</nav>

<script>
  const menuButton = document.getElementById("menu-button");
  const mobileMenu = document.getElementById("mobile-menu");
  const menuIcon = document.getElementById("menu-icon");

  // Verificar que los elementos existen antes de usarlos
  if (menuButton && mobileMenu && menuIcon) {
    // Alternar el menú al hacer clic en el botón
    menuButton.addEventListener("click", () => {
      mobileMenu.classList.toggle("hidden");
      menuIcon.classList.toggle("rotate-90"); // Efecto de rotación al ícono de menú
    });

    // Cerrar el menú si el usuario hace clic fuera de él
    document.addEventListener("click", (event) => {
      const target = event.target as Node; // Convertir event.target a Node

      if (!menuButton.contains(target) && !mobileMenu.contains(target)) {
        mobileMenu.classList.add("hidden");
        menuIcon.classList.remove("rotate-90");
      }
    });

    // Detener la propagación del evento cuando se hace clic en un enlace del menú
    mobileMenu.addEventListener("click", (event) => {
      event.stopPropagation(); // Evita que el clic en el menú se propague al documento
    });
  } else {
    console.error("Uno o más elementos del menú no se encontraron en el DOM.");
  }
</script>
